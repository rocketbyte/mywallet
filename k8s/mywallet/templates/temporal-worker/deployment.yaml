{{- if .Values.worker.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "mywallet.worker.fullname" . }}
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "mywallet.worker.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.worker.replicaCount }}
  selector:
    matchLabels:
      {{- include "mywallet.worker.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "mywallet.worker.selectorLabels" . | nindent 8 }}
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
        checksum/secret: {{ include (print $.Template.BasePath "/secrets.yaml") . | sha256sum }}
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.global.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.worker.securityContext | nindent 8 }}
      initContainers:
        # Wait for MongoDB to be ready
        - name: wait-for-mongodb
          image: busybox:1.36
          command:
            - sh
            - -c
            - |
              echo "Waiting for MongoDB at {{ include "mywallet.mongodb.fullname" . }}:{{ .Values.mongodb.service.port }}..."
              until nc -z {{ include "mywallet.mongodb.fullname" . }} {{ .Values.mongodb.service.port }}; do
                echo "MongoDB is unavailable - sleeping"
                sleep 2
              done
              echo "MongoDB is up - continuing"
        # Wait for Temporal to be ready
        - name: wait-for-temporal
          image: busybox:1.36
          command:
            - sh
            - -c
            - |
              echo "Waiting for Temporal at temporal-frontend.rocket.svc.cluster.local:7233..."
              until nc -z temporal-frontend.rocket.svc.cluster.local 7233; do
                echo "Temporal is unavailable - sleeping"
                sleep 2
              done
              echo "Temporal is up - continuing"
      containers:
        - name: temporal-worker
          image: "{{ .Values.worker.image.repository }}:{{ .Values.worker.image.tag }}"
          imagePullPolicy: {{ .Values.worker.image.pullPolicy }}
          envFrom:
            - configMapRef:
                name: {{ include "mywallet.fullname" . }}-config
            - secretRef:
                name: {{ include "mywallet.fullname" . }}-secrets
          resources:
            {{- toYaml .Values.worker.resources | nindent 12 }}
{{- end }}
