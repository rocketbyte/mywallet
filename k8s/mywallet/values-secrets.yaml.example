# MyWallet Secrets Configuration
# Copy this file to values-secrets.yaml and fill in your actual values
# IMPORTANT: Never commit values-secrets.yaml to git!

secrets:
  # MongoDB credentials
  mongodb:
    rootPassword: "CHANGE-ME-secure-mongodb-password"

  # Gmail OAuth credentials
  # Get these from Google Cloud Console
  gmail:
    clientId: "YOUR-CLIENT-ID.apps.googleusercontent.com"
    clientSecret: "YOUR-GMAIL-CLIENT-SECRET"
    refreshToken: "YOUR-GMAIL-REFRESH-TOKEN"

  # LiteLLM proxy bearer token — must match LITELLM_MASTER_KEY in:
  #   llm-platform/apps/litellm/manifests/secret.yaml (namespace: llm)
  litellm:
    apiKey: "sk-YOUR-LITELLM-MASTER-KEY"

  # OpenAI direct API key (unused — all AI calls go through LiteLLM proxy)
  openai:
    apiKey: ""

  # API docs Basic Auth — protects /docs/playground and /docs/openapi.json
  docs:
    user: "CHANGE-ME-docs-user"
    password: "CHANGE-ME-docs-password"

  # Firebase Admin credentials — required to verify ID tokens on the backend.
  # Get these from: Firebase Console → Project Settings → Service Accounts → Generate new private key.
  # privateKey must be the full PEM, with literal \n sequences preserved.
  firebase:
    projectId: "YOUR-FIREBASE-PROJECT-ID"
    clientEmail: "firebase-adminsdk-xxxxx@YOUR-PROJECT.iam.gserviceaccount.com"
    privateKey: "-----BEGIN PRIVATE KEY-----\nYOUR-KEY-LINES\n-----END PRIVATE KEY-----\n"
    # Set to "true" to skip Firebase verification (local dev only).
    # authBypass: "false"

# Optional: Override default values
# Uncomment and modify as needed

# backend:
#   image:
#     tag: "1.0.0"  # Use specific version instead of latest

# worker:
#   image:
#     tag: "1.0.0"  # Use specific version instead of latest

# mongodb:
#   persistence:
#     size: 20Gi  # Increase storage if needed

# app:
#   gmail:
#     pubSubTopic: "projects/YOUR-GCP-PROJECT/topics/gmail-notifications"
