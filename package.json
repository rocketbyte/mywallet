{
  "name": "mywallet",
  "version": "1.0.0",
  "description": "Expense tracking system with Temporal orchestration",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:api": "npm run dev --workspace=@mywallet/backend-apis",
    "dev:worker": "npm run dev --workspace=@mywallet/temporal-worker",
    "dev": "concurrently \"npm run dev:api\" \"npm run dev:worker\"",
    "build": "npm run build --workspaces",
    "lint": "eslint packages --ext .ts",
    "test": "jest",
    "test:unit": "tsx --test $(find packages -path '*/__tests__/*.test.ts')",
    "seed:patterns": "tsx scripts/seed-email-patterns.ts",
    "seed:schedules": "tsx scripts/seed-daily-analysis-schedules.ts",
    "seed:pipeline": "tsx scripts/seed-pipeline-steps.ts",
    "openspec:sync": "node scripts/openspec-sync.mjs",
    "openspec:check": "node scripts/openspec-sync.mjs check"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@typescript-eslint/eslint-plugin": "^6.13.0",
    "@typescript-eslint/parser": "^6.13.0",
    "concurrently": "^8.2.2",
    "eslint": "^8.54.0",
    "jest": "^29.7.0",
    "mongodb-memory-server": "^11.2.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.2"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
